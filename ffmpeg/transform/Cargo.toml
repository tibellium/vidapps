[package]
name = "ffmpeg-transform"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Media frame transformation for the ffmpeg crate ecosystem"

[dependencies]
ffmpeg-types = { path = "../types" }
